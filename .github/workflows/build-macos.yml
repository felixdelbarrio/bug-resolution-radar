name: Build Binary (macOS)

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-macos:
    if: github.ref == 'refs/heads/master'
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pyinstaller

      - name: Build executable with PyInstaller
        run: |
          pyinstaller --noconfirm --clean --onefile --name bug-resolution-radar --add-data "app.py:." --add-data "assets:assets" --collect-all streamlit --collect-all plotly --collect-all bug_resolution_radar run_streamlit.py

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: bug-resolution-radar-macos
          path: |
            dist/bug-resolution-radar
            .env.example
            assets/
            README.md
          if-no-files-found: error
